eureka:
  client:
    enabled: false

spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
  cloud:
    config:
      enabled: false
      override-none: true
  main:
    banner-mode: 'off'
  output:
    ansi:
      enabled: ALWAYS
  zipkin:
    enabled: false